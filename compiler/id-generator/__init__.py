"""Canonical ID generator stage.

This package's directory name contains a hyphen (per the Sprint 001 deliverable
spec) and is therefore not importable with a normal ``import`` statement. Load
``ids.py`` via ``importlib`` from its file path (see ``compiler/pipeline.py``).
"""
